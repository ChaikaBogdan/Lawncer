/** Tech actions (Invade, Shield) share a fixed range, independent of the caster's equipped weapon. */
export const TECH_RANGE = 3

/** Heat dumped into a target by Invade — a hostile tech attack that pressures their reactor instead of their HP. */
export const INVADE_HEAT = 2

/** How many rounds a Shield grant lasts (same lifecycle as Stunned/Impaired — see decayStatuses). */
export const SHIELD_DURATION = 2
