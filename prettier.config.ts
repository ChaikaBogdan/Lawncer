import type { Options } from 'prettier'

const config: Options = {
  semi: false,
  singleQuote: true,
  tabWidth: 2,
  useTabs: false,
  trailingComma: 'es5',
  printWidth: 100,
  arrowParens: 'always',
}

export default config
